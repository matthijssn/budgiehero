{
  "name": "budgiehero",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bootstrap": "npm run ci:install && npm run build",
    "ci:install": "npm ci --workspaces",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/worker",
    "test": "npm run test -w apps/api && npm run test -w apps/worker",
    "build": "npm run build -w apps/api && npm run build -w apps/worker",
    "dev": "npm run dev -w apps/api"
  }
}